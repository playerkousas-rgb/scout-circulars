# IG 分享圖／Story 出圖 —— 交接文件（2026-09-23）

> 呢份係「下個對話專心做 IG 出圖」用嘅交接。上一個對話已完成嘅嘢、仲欠嘅嘢、已知陷阱、紅線，全部喺度。
> 相關 branch：`arena/01a0c6d4-scout-circulars`（PR #30）。**未合併入 main。**

---

## 0. 一句總結

App 入面已有 **12 款向量設計**嘅 IG 分享圖（即時 canvas，零伺服器），
另外**已備好 9 張 AI 無字底圖**（`story-bases/*.webp`）＋ Python 疊字試樣引擎，但**未接入 app**。
下一個對話嘅核心工作＝**接入**（見 §4）。

---

## 1. 已完成（上線喺 PR #30）

| 項目 | 位置 | 狀態 |
|---|---|---|
| 12 款向量設計 + 縮圖 picker | `index.html`：`POSTER_DESIGNS`（~L2044）、`renderPoster()`（~L2532）、縮圖條 `paintIgDesigns()`（~L2932） | ✅ 已可喺 app IG 分享面板用 |
| 自動內容圖層 | pill（分類）→ 真區徽 → 標題（自動字級）→ 資料卡（截止／對象／費用／名額）→ 日期＋地域行 → 落款 | ✅ 同上 |
| PDF 內文出圖 ＋ 落款帶（廣告位）＋ 全部版數 | `composePdfImage()`、`pdfImageFooter()`、`renderPdfPageBlob()`、`renderAllPdfPages()`；pdf.js 由 CDN 載，`/api/pdf-proxy` 只做 byte bridge | ✅ 已完成（見 §1.2） |
| 分享掣（電腦／手機都分享得到） | 分享面板 `share-sheet`；桌面走「複製＋貼上」 | ✅ 已修好 |
| 出圖台 `?batch=1` | `index.html`：`initBatchView()` 起（~L4200 之後）；支援 `&limit`／`&date`／`&days`／`&mode`／`&design` | ✅ 127 條測試綠 |
| 9 張 AI 無字底圖 | `story-bases/*.webp`（1080×1920，9 張共 1.23 MB） | ✅ 已入 repo，**未接 app** |
| Python 疊字試樣引擎 | `tools/render_story_bases_demo.py`（食真 `cache.json`＋`enrich.json`） | ✅ 出過 9 款試樣 |
| WebP→PNG 轉換器 | `tools/bases_webp_to_png.py` | ✅ |

### 1.2 PDF 內文本機生圖打廣告（2026-09-23 完成）

**落款（廣告位）＝ 分享文案嗰句，一個字都唔改：**
```
---經 通告圖書館 v5.11 整理 @noscout.system
```

落款帶版面（`composePdfImage`）：
- 高度 = 頁寬 **13.5%**（下限 112px）；深藍底 `#0d1626` ＋ 頂部品牌 accent 線 `#3b82f6`
- 第一行：落款（粗體，頁寬 1240px 時 ≈ 51px，色 `#cfe0ff`）
- 第二行：深鏈細字（≈ 27px，`#7c89a3`）＝「完整通告＋最新截止日期：<短網址>」
- 多版通告：右上角細字「第 N / M 版」（≈ 19px，`#5f6d85`）
- **冇區徽格**（精簡路線，唔搶通告版面）
- 任何一步失敗 → 回原頁，唔會因為落款失去張圖

**全部版數（`renderAllPdfPages`）**：分享面板 PDF 區「💾 全部版數」一撳出齊所有版，每版都有落款。
- Chrome／Edge：`showDirectoryPicker({ id: 'pdf-notice-images' })` 揀資料夾一次寫入
- 其他瀏覽器：逐張下載，間隔 320ms（唔會互相取消）
- 檔名：`通告圖書館-<區會><YYYYMMDD>-p<N>.png`（同單版預覽共用 `pdfFileNameFor`）

**共用同一條渲染路**：單版預覽、上下版、全部版數都經 `renderPdfPageBlob(num)`，
所以三邊出嘅圖一定一樣。要改落款只需改 `composePdfImage` 一處。

**未做（如果之後要）**：本機「批次」出 PDF 內文圖（好似 `?batch=1` 咁一次過出今日所有通告嘅首頁＋落款）。

---

## 2. 用戶已定嘅方向（唔好推翻）

1. **繪圖路線唔食 PDF** —— 底圖＋標題／標籤抽取（`cache.json`＋`enrich.json`）已經足夠。
2. **底圖必須零文字** —— 用戶原本嗰 9 張有 `TEMPLATE 2`、假 QR、假日期，唔可以用。
3. **底圖唔可以放 Vercel** —— 9 張佔 1.23 MB，而 budget 只剩 1.00 / 2.00 MB。
   做法：`.vercelignore` 擋住 `story-bases/`，app 由 `raw.githubusercontent` 讀（有 ACAO:\*，配 `crossOrigin='anonymous'` 唔會污染 canvas），jsDelivr 做後備。
4. **LOGO 用真區徽**，唔要假虛線圈（`icons/orgs/orgs.json`，fallback 區→地域→總會）。
5. **標題無資料就唔好出假字**（「詳情見內文」之類一律唔要）。

---

## 3. 三個已修好嘅視覺問題（2026-09-23）

| # | 問題 | 成因 | 修法 |
|---|---|---|---|
| 1 | 字款／字級同 app 唔一致 | Python 版用 `NotoSansTC_900Black`＋自創 ladder（78→70→…→36） | 已改成 app 同一把尺：**96→84→72→64→56→48、行高 1.3、最多 4 行**。**但字體本身仍未統一**（app 用系統 `POSTER_FONT`：PingFang TC／Microsoft JhengHei；Python 用 NotoSansTC）。若要 100% 一樣，要將同一支 TTF 兩邊都用 |
| 2 | 方區徽下面凸個圓圈 | Python 版無條件用圓形托底 | 已改圓角方托底（radius ≈ 18% 邊長，同 app `posterBadge` 一致） |
| 3 | 左上標籤框同字出界 | `textbbox((0,0),…)` 唔計 glyph bearing，字貼邊／出框 | 已改用 glyph 實際 bbox 置中＋padding（`pad_x = 0.62×字級`） |

---

## 4. 下個對話要做嘅（建議次序）

### 4.1 接入 9 款 AI 底圖入 app（核心）

1. **底圖載入器**：`raw.githubusercontent.com/playerkousas-rgb/scout-circulars/main/story-bases/<款>.webp`
   ＋ jsDelivr 後備；`crossOrigin='anonymous'`；**載唔到就自動退回向量款**（唔可以出白圖）。
2. **擴充 `POSTER_DESIGNS`**：加 9 款（`cat`／`kind: 'base'`／`base: '<檔名>'`），
   縮圖條要改成 **async 等底圖 decode**（現時 `paintIgDesigns()` 係同步畫）。
3. **新 `kind: 'base'` 繪圖**：底圖 cover 之後，直接複用現有圖層（pill→徽→標題→資料卡→日期→落款），
   即係唔使重寫，只加一個背景 phase。
4. **字色／底板參數**（已實測，見 §5 表）：每款寫死 `titleColor` ＋ `plate`（要唔要柔邊底板）。
5. **`?batch=1` 自動多一組**：出圖台同 IG 分享都 call 同一個 `renderPoster()`，接入後兩邊都有。

### 4.2 決定未做
- **A：21 款並存**（12 向量 ＋ 9 AI 底圖，用戶自己揀）← 建議
- **B：只留 9 款 AI**（向量款收埋）

### 4.3 之後
- 換色／換風格、每分類多過一款、出 Story 專用版（9:16 已有 `mode:'story'`）

---

## 5. 9 款底圖實測參數（照抄即可）

| 款 | 標題區亮度 | 雜亂度 | 字色 | 要底板？ |
|---|---|---|---|---|
| `train_blue` | 237.0 | 39.2 | `#111111` | 要（雜亂 39） |
| `train_orange` | 240.8 | 13.7 | `#111111` | 唔要 |
| `train_green` | 225.1 | 52.0 | `#111111` | **要**（雜亂 52，唔加一定撞） |
| `competition_gold_black` | 27.1 | 17.3 | `#FFFFFF` | 唔要 |
| `activity_army` | 45.5 | 10.4 | `#FFFFFF` | 唔要 |
| `service_wanted` | 217.5 | 17.1 | `#3D1E00`（深啡）＋米色描邊 `#F2E4C8` | 薄底板 |
| `unc_scope` | 20.1 | 7.7 | `#FFFFFF` | 唔要 |
| `unc_topsecret` | 169.8 | 39.6 | `#111111` | 薄底板 |
| `unc_glitch` | 15.1 | 23.9 | `#FFFFFF` | 唔要 |

判定規則（`tools/render_story_bases_demo.py` 已實作）：
- 亮度 >150 且雜亂 <32 → 跟款原色，唔加底板
- 亮度 >150 且雜亂 ≥38 → 深字＋半透明柔邊底板（Gaussian blur 56px，alpha 170～214）
- 亮度 <95 → 白字＋黑描邊
- 中間調 → 亮度 >122 用深字，否則白字

---

## 6. 佈局座標（app 現行，可直接複用）

```js
POSTER_W = 1080, POSTER_H = 1350 (feed 4:5), POSTER_STORY_H = 1920 (story), POSTER_MX = 60
pill  : x=60, y=80, 高 60, 字 32px
badge : x=844, y=68, 176×176（圓角方托底 radius≈32）
zone  : x0=60, x1=1020, y0=safeTop+210, y1=cardsTop-114
標題   : 96→84→72→64→56→48，行高 1.3×，最多 4 行
資料卡 : rowH=108, cardH=92；頒佈日期行永遠有
落款   : creditY = H-46
```

---

## 7. 紅線（唔可以踩）

1. **唔准伺服器端出圖**（零 function、零 storage、零上傳）
2. **Vercel 唔准加 Python 依賴**（`requirements.txt` 保持零依賴）
3. **上載 budget 2 MB**：而家 1.00 MB；任何大檔要 `.vercelignore` 或搬去 raw
4. **唔准削弱 bundle guard 嘅檢查 4**（`icons/icon-512.png`／`icon-maskable-512.png` 兩個豁免唔可以改）
5. **IG Story 發佈唔可以自動化**：Graph API 貼嘅 Story 加唔到 link sticker，「撳 Story→專屬頁」個閉環會斷。出圖可以自動，發佈一定人手。

---

## 8. 測試同驗證

```bash
node test_share_branch.js                 # 127 ✅（IG 12 款、PDF 落款、分享掣、出圖台）
python3 .github/scripts/vercel_bundle_guard.py   # 必須五項全綠、上載 ≤2 MB
python3 tools/render_story_bases_demo.py  # 9 款 AI 底圖試樣（需 pip install pillow）
node test_share_branch.js 之類測試冇覆蓋 canvas 真繪圖 → 要另開 jsdom ＋ @napi-rs/canvas 才驗到
```

**已知環境陷阱**
- 本地 Chromium 起唔到（puppeteer 下載被 reset、@sparticuz/chromium 缺 `libnspr4.so`、apt 唔通）→ 要真 canvas 驗證就要喺 CI（scrape.yml 已裝 Chromium）
- jsdom 要喺 repo 內 `npm i --no-save jsdom` 先 require 到（唔好喺 `/tmp` 行）
- sandbox 重啟會清 `/tmp` 同 `node_modules`（`~/.cache/fonts` 都會冇）→ 字體要重新 `npm pack @expo-google-fonts/noto-sans-tc`

---

## 9. 檔案地圖

| 路徑 | 內容 |
|---|---|
| `index.html` | 12 款設計、`renderPoster()`、IG 分享面板、PDF 出圖＋落款帶、`?batch=1` 出圖台 |
| `story-bases/*.webp` | 9 張 AI 無字底圖（1080×1920） |
| `story-bases/demo/` | 9 款實樣（真通告＋真區徽）＋ `_sheet.jpg` 並列對比 |
| `tools/render_story_bases_demo.py` | Python 疊字試樣引擎（自動字色／底板） |
| `tools/bases_webp_to_png.py` | WebP→PNG（畀用戶自己嘅 Pillow 腳本） |
| `icons/orgs/orgs.json` ＋ `*-256.png` | 真區徽（PNG 版同時係 Pillow 用；`.vercelignore` 只擋 `-256.png`，AVIF 留低俾 app） |
| `tools/render_story_templates.py` | Actions 每日 9 款 Pillow 草稿（push 去 `stories` 分支） |
| `story_queue.py` | 揀當日通告嘅唯一真源（`batchQueueRange` 已對齊，逐欄一致） |
